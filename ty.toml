# ty.toml
# Astral ty configuration for markov-chain-monte-carlo
# Check all discovered Python files, including Semgrep fixtures.
# Scientific notebooks also use the shared native notebook checks.

[terminal]
# Keep output concise by default.
output-format = "concise"
